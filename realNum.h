#include <iostream>
#include <string>

using namespace std;

class realNum
{
	protected:

		string user;
	
	public:
	
		realNum();	
		bool realNumber(const string &);
		bool checkTheNumber(const char &);
};
